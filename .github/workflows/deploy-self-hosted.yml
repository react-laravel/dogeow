# 推送 main 后在国内自托管 Runner 上通过 Deployer 执行零停机部署。
# 前提：目标服务器配好 Node.js / npm / PM2 / Nginx。
# 详情见 docs/DEPLOYMENT.md。
name: Deploy (Self-hosted)

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5

      - name: Ensure Deployer phar present
        run: |
          set -euo pipefail
          DEP_BIN="$HOME/.deployer/dep.phar"
          DEP_VERSION="7.5.7"
          if [ ! -f "$DEP_BIN" ]; then
            mkdir -p "$(dirname "$DEP_BIN")"
            curl -fsSL "https://deployer.org/releases/v${DEP_VERSION}/deployer.phar" -o "$DEP_BIN"
            chmod +x "$DEP_BIN"
          fi
          php "$DEP_BIN" --version

      - name: Deploy production
        run: |
          set -euo pipefail
          unset RUNNER_TRACKING_ID
          php "$HOME/.deployer/dep.phar" deploy production -v
        env:
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          APP_ROOT: ${{ secrets.APP_ROOT }}
          PM2_APP: ${{ secrets.PM2_APP }}
          DOGEOW_API_CURRENT_PATH: ${{ secrets.DOGEOW_API_CURRENT_PATH }}
          DEPLOY_USER: www-data
