/** 物品图片容器：透明底，避免 object-contain 留白变成白边 */
export const THING_IMAGE_FRAME_CLASS =
  'flex items-center justify-center overflow-hidden bg-transparent'

/** 物品图片：保持原比例，透明底 */
export const THING_IMAGE_CLASS = 'max-h-full max-w-full bg-transparent object-contain object-center'
